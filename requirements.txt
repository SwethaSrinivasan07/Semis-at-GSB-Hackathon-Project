# SupplyLine backend dependencies.
# Frontend (Streamlit / etc.) may pin additional packages of its own.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
python-dotenv>=1.0
requests>=2.31
pydantic>=2.6

# Report generation
reportlab>=4.0
openpyxl>=3.1

# Convenience for BOM ingest / interop (used by main.py & teammates)
pandas>=2.2
